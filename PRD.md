# PRD: Markdown 转公众号排版工具

## 1. 概述

### 1.1 产品名称
MD2WeChat — Markdown 转公众号排版工具

### 1.2 产品定位
一个纯前端的 Markdown 到微信公众号富文本排版工具，支持实时预览、多主题切换、样式微调，一键复制后可直接粘贴到公众号编辑器。

### 1.3 目标用户
内部团队成员（编辑、运营、技术人员）

### 1.4 核心价值
- **效率**：Markdown 写作 → 一键转换 → 直接粘贴到公众号，省去手动排版
- **一致性**：预设主题保证团队公众号文章风格统一
- **零门槛**：单 HTML 文件，无需安装任何依赖

---

## 2. 功能需求

### 2.1 编辑器（左栏）

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F1.1 | Markdown 编辑区 | 左栏为纯文本编辑器，支持 Markdown 输入 | P0 |
| F1.2 | 实时同步 | 编辑内容实时渲染到右栏预览区 | P0 |
| F1.3 | 内置示例文章 | 首次打开自动加载一篇示例 Markdown 文章，覆盖常用语法 | P0 |
| F1.4 | 内容持久化 | 编辑内容自动存入 localStorage，刷新/重开不丢失 | P0 |

### 2.2 预览区（右栏）

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F2.1 | 富文本预览 | 右栏实时展示公众号排版效果 | P0 |
| F2.2 | 所见即所得 | 预览效果与粘贴到公众号后的效果一致 | P0 |

### 2.3 Markdown 语法支持

| ID | 语法 | 说明 | 优先级 |
|----|------|------|--------|
| F3.1 | 标题 | h1 - h6 | P0 |
| F3.2 | 加粗/斜体 | `**bold**` / `*italic*` | P0 |
| F3.3 | 有序/无序列表 | 支持嵌套 | P0 |
| F3.4 | 引用块 | `>` blockquote，带左侧色条样式 | P0 |
| F3.5 | 代码块 | 带背景色的样式块，**无语法高亮** | P0 |
| F3.6 | 行内代码 | 带背景色的行内样式 | P0 |
| F3.7 | 图片 | 渲染 `![alt](url)` 为 `<img>` 展示预览 | P0 |
| F3.8 | 链接 | 渲染为可点击链接或脚注样式 | P0 |
| F3.9 | 表格 | 标准 Markdown 表格渲染 | P0 |
| F3.10 | 分割线 | `---` / `***` | P1 |
| F3.11 | 删除线 | `~~text~~` | P1 |

### 2.4 主题系统

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F4.1 | 主题切换 | 提供 4 套预设主题，下拉选择切换 | P0 |
| F4.2 | 实时生效 | 切换主题后预览区即时刷新 | P0 |
| F4.3 | 主题持久化 | 当前选中主题存入 localStorage | P0 |

**预设主题：**

| 主题名 | 风格描述 |
|--------|---------|
| 简约 | 黑白灰为主，清爽留白，适合通用内容 |
| 科技 | 深色代码块、蓝色强调色，适合技术文章 |
| 文艺 | 暖色调、衬线感字体样式、柔和间距 |
| 商务 | 正式感、深蓝/深灰主色、紧凑排版 |

### 2.5 样式微调

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F5.1 | 正文字号 | 可调范围 14-20px，默认 16px | P0 |
| F5.2 | 行间距 | 可调范围 1.4-2.2，默认 1.75 | P0 |
| F5.3 | 主题色 | 颜色选择器，影响标题、引用、链接等强调色 | P0 |
| F5.4 | 代码块配色 | 背景色 + 文字色可调 | P0 |
| F5.5 | 设置持久化 | 所有微调设置存入 localStorage | P0 |

### 2.6 一键复制

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F6.1 | 复制按钮 | 点击后将预览区 HTML 复制到剪贴板 | P0 |
| F6.2 | 富文本复制 | 使用 `clipboard.write` 写入 `text/html`，确保粘贴到公众号后格式保留 | P0 |
| F6.3 | 复制反馈 | 复制成功后按钮文字变为"已复制 ✓"，2 秒恢复 | P0 |

---

## 3. 非功能需求

### 3.1 技术约束

| 约束 | 说明 |
|------|------|
| 单文件 | 整个应用为单个 HTML 文件，CSS/JS 全部内联 |
| 零依赖 | 不引入任何外部 CDN、JS 库（Markdown 解析自行实现或内联轻量库） |
| 存储 | 全部使用 localStorage，无后端 |

### 3.2 Markdown 解析策略

由于"零外部依赖"约束，Markdown 解析有两个方案：

- **方案 A**：内联一个轻量 Markdown 解析库（如 marked.js minified，约 30KB）直接嵌入 HTML
- **方案 B**：自行实现简化版 Markdown 解析器

**推荐方案 A** — 内联 marked.js。理由：覆盖语法全面、稳定性好、单文件内联不算外部依赖。

### 3.3 浏览器兼容

- 目标：Chrome 最新版（内部团队使用，不做宽泛兼容）
- 复制功能依赖 Clipboard API，需 HTTPS 或 localhost

### 3.4 部署

- **Phase 1**：产出单 HTML 文件，双击可用
- **Phase 2**：通过 GitHub Pages 部署到线上

---

## 4. UI 布局

```
┌─────────────────────────────────────────────────┐
│  [主题: ▼简约]  [字号: 16] [行距: 1.75]         │
│  [主题色: 🟦]  [代码背景: ⬛] [代码文字: ⬜]     │
│                              [📋 复制到公众号]   │
├────────────────────┬────────────────────────────┤
│                    │                            │
│   Markdown 编辑器   │     公众号排版预览          │
│   (纯文本输入)      │     (富文本渲染)            │
│                    │                            │
│                    │                            │
│                    │                            │
└────────────────────┴────────────────────────────┘
```

- 顶部工具栏：主题选择 + 样式微调 + 复制按钮
- 左栏：Markdown 编辑器（约 50% 宽度）
- 右栏：实时预览（约 50% 宽度）
- 响应式：窗口较窄时可上下堆叠（P1，非必须）

---

## 5. 示例文章内容要求

内置示例文章应覆盖以下语法，便于用户打开即看到完整效果：

- h1/h2/h3 标题
- 加粗、斜体
- 有序列表、无序列表（含嵌套）
- 引用块
- 代码块（多行）
- 行内代码
- 表格
- 图片（使用 placeholder 图片 URL）
- 链接
- 分割线

---

## 6. 验收标准

验收分三个层级，按优先级递进：

### L1 — 基础功能（必须全部通过才能进入 L2）

核心功能是否可用。

| ID | 验收项 | 测试步骤 | 通过条件 |
|----|--------|---------|---------|
| L1.1 | 页面加载 | 双击 HTML 文件在 Chrome 打开 | 页面正常加载，无白屏、无 JS 报错，左右分栏布局正确显示 |
| L1.2 | 示例文章展示 | 首次打开页面（无 localStorage 缓存） | 左栏自动填充示例 Markdown 文章，右栏显示对应排版预览 |
| L1.3 | 标题渲染 | 在左栏输入 `# h1`、`## h2`、`### h3` | 右栏分别渲染为对应大小的标题，字号递减，有明确层级区分 |
| L1.4 | 加粗/斜体 | 输入 `**加粗**` 和 `*斜体*` | 右栏正确渲染加粗和斜体样式 |
| L1.5 | 列表渲染 | 输入无序列表（`- item`）和有序列表（`1. item`），含一层嵌套 | 右栏正确渲染列表符号/编号，嵌套缩进正确 |
| L1.6 | 引用块 | 输入 `> 这是引用` | 右栏渲染带左侧色条的引用块样式 |
| L1.7 | 代码块 | 输入三反引号包裹的多行代码 | 右栏渲染为带背景色的样式块（非白底），等宽字体，无语法高亮 |
| L1.8 | 行内代码 | 输入 `` `code` `` | 右栏渲染为带背景色的行内样式 |
| L1.9 | 表格 | 输入标准 Markdown 表格（至少 2 列 3 行） | 右栏渲染为带边框的表格，表头与数据行可区分 |
| L1.10 | 图片 | 输入 `![alt](https://via.placeholder.com/200)` | 右栏显示图片预览 |
| L1.11 | 链接 | 输入 `[文本](https://example.com)` | 右栏渲染为可识别的链接样式（带颜色/下划线） |
| L1.12 | 实时预览 | 在左栏连续输入文字 | 右栏在 500ms 内实时更新，无明显卡顿 |
| L1.13 | 一键复制 | 点击"复制"按钮 | ① 按钮文字变为"已复制 ✓"并在 2 秒内恢复；② 粘贴到富文本编辑器（如公众号后台）格式保留 |

### L2 — 交互体验（L1 全部通过后验收）

主题、样式微调和持久化是否正常工作。

| ID | 验收项 | 测试步骤 | 通过条件 |
|----|--------|---------|---------|
| L2.1 | 主题切换 — 简约 | 选择"简约"主题 | 预览区立即切换为黑白灰色调、清爽留白风格 |
| L2.2 | 主题切换 — 科技 | 选择"科技"主题 | 预览区立即切换为蓝色强调色、深色代码块风格 |
| L2.3 | 主题切换 — 文艺 | 选择"文艺"主题 | 预览区立即切换为暖色调、柔和间距风格 |
| L2.4 | 主题切换 — 商务 | 选择"商务"主题 | 预览区立即切换为深蓝/深灰、紧凑正式风格 |
| L2.5 | 字号调整 | 将正文字号从 16px 调为 14px，再调为 20px | 预览区正文字号实时变化，14px 明显更小，20px 明显更大 |
| L2.6 | 行距调整 | 将行距从 1.75 调为 1.4，再调为 2.2 | 预览区行间距实时变化，1.4 明显更紧凑，2.2 明显更松 |
| L2.7 | 主题色调整 | 通过颜色选择器将主题色改为红色 | 预览区标题、引用色条、链接等强调色变为红色 |
| L2.8 | 代码块配色 | 修改代码块背景色为深蓝，文字色为白色 | 预览区代码块背景变深蓝、文字变白色 |
| L2.9 | 内容持久化 | 编辑 Markdown 内容后刷新页面 | 刷新后左栏内容恢复为刷新前的编辑内容（非示例文章） |
| L2.10 | 主题持久化 | 选择"科技"主题后刷新页面 | 刷新后主题仍为"科技"，预览效果一致 |
| L2.11 | 样式设置持久化 | 修改字号为 18px + 行距为 2.0 后刷新页面 | 刷新后字号仍为 18px，行距仍为 2.0 |
| L2.12 | 主题 + 微调联动 | 先选"简约"主题，再手动调主题色为绿色 | 主题色被覆盖为绿色，其他简约风格保持不变 |

### L3 — 高级场景（L2 全部通过后验收）

边界 case、公众号实际兼容性、复杂内容。

| ID | 验收项 | 测试步骤 | 通过条件 |
|----|--------|---------|---------|
| L3.1 | 公众号实际粘贴 | 点击复制 → 打开微信公众号后台编辑器 → Ctrl+V 粘贴 | 标题/加粗/列表/引用/代码块/表格格式保留，与预览基本一致 |
| L3.2 | 长文章性能 | 输入超过 500 行的 Markdown 内容 | 编辑和预览仍流畅，无明显卡顿（输入到渲染 < 1s） |
| L3.3 | 空内容 | 清空左栏所有内容 | 右栏预览区为空白，无报错 |
| L3.4 | 特殊字符 | 输入包含 `<script>`、`<div>`、HTML 实体（`&amp;`）、emoji 的文本 | 不执行脚本，HTML 标签被正确转义或安全渲染，emoji 正常显示 |
| L3.5 | 复杂嵌套 | 输入引用内含列表、列表内含代码块的嵌套结构 | 渲染层级正确，不出现排版错乱 |
| L3.6 | 表格对齐 | 输入带左对齐/居中/右对齐的 Markdown 表格 | 对齐方式正确渲染 |
| L3.7 | 多次复制 | 连续点击复制按钮 3 次 | 每次都正常复制，按钮反馈正常，无报错 |
| L3.8 | 分割线 | 输入 `---` 或 `***` | 右栏渲染为水平分割线 |
| L3.9 | 删除线 | 输入 `~~删除文字~~` | 右栏渲染为带删除线的文字 |
| L3.10 | localStorage 容量 | 持续输入直到内容超过 1MB | 不出现崩溃，若超限应有友好提示或静默降级 |

---

## 7. 里程碑

| 阶段 | 内容 | 产出 | 验收范围 |
|------|------|------|---------|
| M1 | PRD 确认 | 本文档 ✅ | — |
| M2 | 开发实现 | 单 HTML 文件 | — |
| M3 | L1 验收 | L1 基础功能全部通过 | L1.1 - L1.13 |
| M4 | L2 验收 | L2 交互体验全部通过 | L2.1 - L2.12 |
| M5 | L3 验收 | L3 高级场景全部通过 | L3.1 - L3.10 |
| M6 | 线上部署 | GitHub Pages 上线 | 线上环境回归 |

---

## 8. 风险与决策记录

| 风险 | 影响 | 应对 |
|------|------|------|
| Markdown 解析复杂度 | 自实现可能语法覆盖不全 | 推荐内联 marked.js |
| 公众号样式兼容 | 公众号编辑器会过滤部分 CSS | 使用内联 style 而非 class |
| 图片外链限制 | 公众号不支持外链图片 | 本工具仅做预览，用户需自行上传图片到公众号 |

**关键决策：**
- 公众号渲染必须使用**内联 style**（不能用 class），因为公众号编辑器会剥离 `<style>` 标签和 class
- 代码块使用带背景色的 `<section>` + `<pre>` 样式块，不做语法高亮

---

*PRD by Pichai | 2026-04-08*
